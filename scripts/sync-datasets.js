/**
 * Manual fallback — only needed outside of a running Metro server
 * (e.g. CI, first clone, or before running tests).
 *
 * During normal development, metro.config.js handles this automatically.
 */
require('../metro.config.js');
